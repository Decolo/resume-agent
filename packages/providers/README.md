# packages/providers

Provider adapter package.

Ownership:

1. Provider implementations live in `packages/providers/resume_agent_providers/*`.
2. Providers remain isolated from `apps/*` and `packages/core/*`.
3. Legacy `resume_agent/providers/*` compatibility wrappers have been retired.
